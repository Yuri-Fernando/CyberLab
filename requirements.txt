# =============================================================================
# CyberLab - Dependências Python (testado em Python 3.10)
# Instale com: pip install -r requirements.txt
# =============================================================================

# --- Núcleo científico / ML ---
numpy>=1.23.0
pandas>=1.5.0
scipy>=1.9.0
scikit-learn>=1.3.0

# --- Visualização / Dashboards ---
matplotlib>=3.6.0
seaborn>=0.12.0
plotly>=5.14.0

# --- Análise de mídia sintética (voz e imagem) ---
librosa>=0.10.0          # MFCC / análise espectral (voice cloning detection)
soundfile>=0.12.0        # I/O de áudio
opencv-python>=4.8.0     # análise de imagem
pillow>=10.0.0           # ELA / forense de imagem

# --- Jupyter / notebooks ---
jupyter>=1.0.0
jupyterlab>=3.5.0
ipykernel>=6.0.0

# --- Redes e segurança (laboratório clássico v1.0) ---
python-nmap>=0.7.1
paramiko>=3.0.0
requests>=2.28.0

# --- Criptografia (ransomware simulado) ---
cryptography>=39.0.0

# --- Captura de entrada (keylogger simulado) ---
pynput>=1.7.0

# --- Utilitários ---
python-dotenv>=0.21.0
pyyaml>=6.0
tqdm>=4.64.0
tabulate>=0.9.0

# --- Testes / qualidade ---
pytest>=7.0.0

# =============================================================================
# NOTA: Os ataques adversariais (FGSM/PGD), DP-SGD, model extraction/inversion,
# membership inference e drift detection são implementados DO ZERO em numpy/
# scikit-learn neste projeto (sem dependência de ART/Foolbox/Opacus/Evidently),
# para fins didáticos e portabilidade. As bibliotecas de mercado equivalentes são
# citadas na documentação como referência de produção:
#   - adversarial-robustness-toolbox (ART) / foolbox  -> ataques adversariais
#   - opacus / tensorflow-privacy                      -> DP-SGD
#   - evidently / whylogs                              -> monitoramento de drift
# =============================================================================
